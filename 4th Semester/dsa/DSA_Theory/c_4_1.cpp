#include <iostream>
using namespace std;
struct node{
        struct node *next;
        int element;
    };
void add(int toAdd, struct node *node)
{
    if(node->next == NULL)
    {
        
    }
}
int main()
{
    struct node *node = NULL;

}
